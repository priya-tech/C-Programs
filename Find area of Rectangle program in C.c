#include<stdio.h>
int main()
{
    float result,length,breadth;
    printf("Enter the length and breadth to find rectangle area ");
    scanf("%f%f",&length,&breadth);
    result=length*breadth;
    printf("\n The rectangle area is %f",result);
}
